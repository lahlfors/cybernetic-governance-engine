models:
  - type: main
    engine: google_vertex_ai
    model: gemini-pro

rails:
  input:
    flows:
      - input rails
  output:
    flows:
      - check trade amount source
      - check trade safety

instructions:
  - type: general
    content: |
      You are a financial advisor assistant. You must ensure all trade requests come directly from the user.

python_modules:
  - src.governance.nemo_actions
