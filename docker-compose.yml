version: '3.8'

services:
<<<<<<< HEAD
  # OPA: Standalone Service (Simulating Remote Deployment)
  opa:
    image: openpolicyagent/opa:latest-static
=======
  opa:
    image: openpolicyagent/opa:latest-static
    # Run OPA listening on both UDS (for sidecar simulation) and TCP (for debugging/fallback)
>>>>>>> origin/docs/agentic-gateway-analysis-15132879769016669359
    command:
      - "run"
      - "--server"
      - "--log-level=info"
<<<<<<< HEAD
      - "--addr=:8181" # TCP Only (Remote simulation)
      - "/policies"
    volumes:
      - ./policies:/policies
=======
      - "--addr=unix:///tmp/opa.sock"
      - "--addr=:8181"
      - "/policies"
    volumes:
      - ./policies:/policies
      # Shared volume for Unix Domain Socket
      - opa_socket_vol:/tmp
>>>>>>> origin/docs/agentic-gateway-analysis-15132879769016669359
    ports:
      - "8181:8181"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8181/health"]
      interval: 5s
      timeout: 3s
      retries: 5

<<<<<<< HEAD
  # NeMo Guardrails: Retained
=======
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

>>>>>>> origin/docs/agentic-gateway-analysis-15132879769016669359
  nemo:
    build:
      context: .
      dockerfile: Dockerfile.nemo
    command: ["uvicorn", "src.governance.nemo_server:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - GOOGLE_CLOUD_PROJECT=${GOOGLE_CLOUD_PROJECT}
    volumes:
      - ./src:/app/src
<<<<<<< HEAD
    # Removed Redis dependency

  # Main Agent Service (ADK Native)
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GOOGLE_CLOUD_PROJECT=${GOOGLE_CLOUD_PROJECT}
      - GOOGLE_CLOUD_LOCATION=${GOOGLE_CLOUD_LOCATION:-us-central1}
      # Point to Remote OPA (simulated via Docker network)
      - OPA_URL=http://opa:8181/v1/data/finance/allow
      - OPA_AUTH_TOKEN=${OPA_AUTH_TOKEN:-dev-token}
      - GOOGLE_GENAI_USE_VERTEXAI=true
    volumes:
      - ./src:/app/src
      - ./.env:/app/.env
    depends_on:
      - opa
      - nemo

volumes:
  # Removed redis_data and opa_socket_vol
=======
    depends_on:
      - redis

  # Optional: Application service placeholder (if running fully in docker)
  # app:
  #   build: .
  #   volumes:
  #     - opa_socket_vol:/tmp
  #   environment:
  #     - OPA_URL=http+unix://%2Ftmp%2Fopa.sock/v1/data/finance/decision
  #     - REDIS_URL=redis://redis:6379

volumes:
  redis_data:
  opa_socket_vol:
>>>>>>> origin/docs/agentic-gateway-analysis-15132879769016669359
