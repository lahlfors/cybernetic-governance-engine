# NeMo Guardrails Configuration

models:
  - type: main
    engine: vllm_llama
    model: gs://laah-cybernetics-models/meta-llama/Meta-Llama-3.1-8B-Instruct

colang_version: "2.x"

instructions:
  - type: general
    content: |
      You are a helpful financial advisor assistant.
      You must adhere to strict safety guidelines.

rails:
  config:
    sensitive_data_detection:
      include_rails: true
      input:
        entities:
          - EMAIL_ADDRESS
          - PHONE_NUMBER
          - PERSON
          - CREDIT_CARD
          - US_SSN
      output:
        entities:
          - EMAIL_ADDRESS
          - PHONE_NUMBER
          - PERSON
          - CREDIT_CARD
          - US_SSN

    flows:
      - definitions.co
      - main_logic.co

# Actions are registered programmatically in nemo_manager.py
