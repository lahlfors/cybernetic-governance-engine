apiVersion: v1
kind: Service
metadata:
  name: vllm-service
  namespace: governance-stack
  labels:
    app: vllm-inference
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: vllm-inference
  type: ClusterIP
