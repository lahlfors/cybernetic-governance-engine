# Fill in your GCP project info and rename this file to ".env".

# --- Model Configuration ---
# Fast path: Supervisor, Data Analyst, Execution Analyst (Low Latency)
MODEL_FAST=gemini-2.5-flash-lite

# Reasoning path: Risk Analyst, Verifier, Consensus (Safety Critical)
MODEL_REASONING=gemini-2.5-pro

# --- Vertex AI Configuration (Required) ---
GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_PROJECT=<YOUR_PROJECT_ID>
GOOGLE_CLOUD_LOCATION=<YOUR_PROJECT_LOCATION> # e.g., us-central1
GOOGLE_CLOUD_STORAGE_BUCKET=<YOUR_STORAGE_BUCKET> # Only required for Agent Engine deployment

# --- Governance & Policy ---
# OPA Policy Engine (Sidecar or External Service)
# For Local Dev (Docker): http://localhost:8181/v1/data/finance/decision
# For Cloud Run (Service URL): https://opa-service-xxxx.run.app/v1/data/finance/decision
OPA_URL=http://localhost:8181/v1/data/finance/decision

# NeMo Guardrails (Internal)
# No external URL needed. NeMo runs in-process within the Gateway.
# Requires 'en_core_web_sm' Spacy model (installed via Docker/uv).

# --- Observability (LangSmith / Langfuse / OTel) ---

# LangSmith (Preferred for Agent Tracing)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_ENDPOINT="https://api.smith.langchain.com"
LANGCHAIN_API_KEY=<YOUR_LANGSMITH_API_KEY>
LANGCHAIN_PROJECT="financial-advisor"

# Langfuse (Optional / Legacy)
LANGFUSE_PUBLIC_KEY=<YOUR_PUBLIC_KEY>
LANGFUSE_SECRET_KEY=<YOUR_SECRET_KEY>
LANGFUSE_HOST=https://cloud.langfuse.com

# OpenTelemetry (Infrastructure Tracing)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer <TOKEN>

# --- Service Configuration ---
PORT=8080
GOOGLE_APPLICATION_CREDENTIALS= # Path to service account key (optional for local dev if gcloud auth application-default login is used)
